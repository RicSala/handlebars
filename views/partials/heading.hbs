<div class='text-center'>
    <div class="text-2xl font-bold {{textColor type}}"> {{{title}}}</div>
    <div class="font-light text-neutral-500 mt-2">{{{subtitle}}}

        {{test 'hello'}}
        {{ textColor type}}
    </div>
</div>